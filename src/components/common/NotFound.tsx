import * as React from 'react';

export interface NotFoundProps {
}
//vi dung index export nen dung tsrpfc , goi la export name
export function NotFound(props: NotFoundProps) {
  return (
    <div>
      NotFOund
    </div>
  );
}

